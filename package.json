{
  "name": "controle-de-estoque",
  "version": "1.0.0",
  "private": true,
  "description": "Controle de estoque MOVA com SQLite",
  "scripts": { "start": "node painel/server.mjs", "check": "node --check painel/dist/app.js && node --check painel/server.mjs && node --check painel/database.mjs && node --check painel/http-app.mjs", "test": "node --test painel/tests/*.test.mjs" },
  "engines": { "node": ">=24.0.0" }
}
